import { getPartOneSolution, getPartTwoSolution } from '../days/day20';
import Day from './day';

export default function Day20() {
  const props = { day: 20, getPartOneSolution, getPartTwoSolution };
  return <Day {...props} />;
}
