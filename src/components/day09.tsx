import { getPartOneSolution, getPartTwoSolution } from '../days/day09';
import Day from './day';

export default function Day09() {
  const props = { day: 9, getPartOneSolution, getPartTwoSolution };
  return <Day {...props} />;
}
